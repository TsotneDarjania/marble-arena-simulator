import { GameModeType, MatchInfoType } from "../types/gameTypes";

export const gameConfig : {gameMode : GameModeType} = {
  gameMode : "friendly"
} 

export const matchInfo = {
  matchTitle: "",
  matchSubTitle: "",
} as MatchInfoType;
